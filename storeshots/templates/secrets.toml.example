# Per-project API keys — copy into YOUR APP REPO as storeshots/secrets.toml
# (NOT in utilities/storeshots — that folder is the CLI source only)
#
#   cd ~/projects/my-app
#   cp path/to/secrets.toml.example storeshots/secrets.toml
#
# NEVER commit storeshots/secrets.toml

openrouter = "sk-or-v1-..."
gemini = "..."
